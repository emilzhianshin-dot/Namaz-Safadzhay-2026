# Намаз Вакытлары 1.2 — проверка проекта

Дата: 12 сентября 2026. applicationId: ru.namaz.safadzhay. versionName: 1.2. versionCode: 31.

## Изменения

За основу взят последний архив Namaz_Vakytylary_v1.2_EXACT_FROM_WORKING_APK_1.1.zip, сверенный с предоставленным Namaz_Vakytylary_1.1_SOURCE(2).zip. В прежнем архиве изменили только app/build.gradle.kts, но preflight.py и verify-apk.sh всё ещё требовали 1.1 / 30. Это останавливало проверки до сборки. Теперь оба скрипта проверяют 1.2 / 31, а GitHub Actions выдаёт файл Namaz_Vakytylary_1.2.apk.

Код приложения, AndroidManifest.xml, Кыбла, настройки, тесты, изображения и остальные ресурсы побайтно совпадают с рабочими исходниками 1.1. Новых функций или изменений поведения нет. Инструкция обновлена, gradlew имеет право запуска. Старые отчёты перенесены в baseline-1.1 с сохранением содержимого.

## Выполнено в этой сессии

- Preflight: PASS. Сверены 306 дней и 1530 времён Сафаджая и Москвы за август–декабрь 2026, разрешения, идентификатор и версия, ресурсы, XML, контрольная сумма Gradle Wrapper, синтаксис shell и обязательные шаги CI.
- Сверка исходников приложения и ресурсов с рабочей версией 1.1: PASS.
- SHA-256 предоставленного APK 1.1 совпадает с сохранённым отчётом сборки 1.1.
- Сертификат ключа app/namaz-release.jks совпадает с сертификатом из предоставленного APK 1.1 и эталонным release-certificate.sha256. Ключ сохранён побайтно.

## Сборка не завершена

Запущена команда bash gradlew --no-daemon :app:testReleaseUnitTest :app:lintRelease :app:assembleRelease. Gradle Wrapper остановился при загрузке gradle-8.10.2-bin.zip: java.net.SocketException: Network is unreachable. Android SDK в текущей среде отсутствует.

Нового APK 1.2 нет; unit-тесты, Android Lint, проверка подписи нового APK и установка на телефон в этой сессии не выполнялись. Результаты в baseline-1.1 относятся только к предыдущей версии и не подтверждают новую сборку. Направление компаса, звук, вибрация и доставка уведомлений на физическом устройстве здесь не проверены.

Проект сохраняет applicationId и ключ рабочей версии 1.1 и повышает код версии с 30 до 31. После успешной сборки и проверки APK это позволяет обновить предоставленную версию без удаления. Фактически установленная на телефоне версия не исследовалась.

Для завершения сборки нужны Java 17, Android SDK 35, Build Tools 35.0.0 и доступ к репозиториям зависимостей. Команды локальной сборки без GitHub приведены в README.md; workflow также сохранён.
